-- ============================================================
-- 일정 라이프사이클: "작업 결과" 필드 추가
-- 플로우: 접수 → 배정 → 진행 → 진행완료 → [결과 기록] → 만족도 체크
-- ============================================================

ALTER TABLE schedules
  ADD COLUMN IF NOT EXISTS work_result TEXT NOT NULL DEFAULT '';
